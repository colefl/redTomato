<div class="alarm"

    @bind="@TimerValue"
    @bind:event="onInput">

<p>Minutes: </p> <input type="input" @onInput="calcTotalTime"/>
<p>Minutes: </p> <input type="input" @onInput="calcTotalTime"/>
<p>Seconds: </p> <input type="input" @onInput="calcTotalTime"/>

</div>
    
<p> Seconds total: @TimerValue</p>

<style>
</style>

@code {

    [Parameter]
    public int TimerValue {get; set; } //I believe these are parameters that must be defined for a new alarm instance upon some stack overflow scrolling

    [Parameter]
    public EventCallback<int> TimerValueChanged {get; set; }

    public void calcTotalTime(){
        //TimerValue = (Hours * 3600) + (Minutes * 60) + Seconds;
        //TimerValue = 10;
        InvokeAsync(StateHasChanged);
    }
}